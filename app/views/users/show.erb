<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%=@user.username%></title>
  </head>
  <body>
    <h1><%=@user.username%>'s Wishlist and Attractions</h1>
    <h3><%= @user.wishlist.name %></h3>
    <% @user.wishlist.each do |attraction| %>
      <p><%= attraction.name %></p>
    <% end %>
  </body>
</html>
