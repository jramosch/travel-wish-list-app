<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%=@attraction.name%></title>
  </head>
  <body>
    <h2><%=@attraction.name%></h2>
    <p>Submitted by: <%=@attraction.user.username%></p>
    <p>City: <%=@attraction.city.name%><br>
    <%=@attraction.description%></p>
    <br>
    <% if @current_user == @attraction.user %>
    <a href="/attractions/<%=@attraction.id%>/edit">Edit Attraction</a>
    <% end %>
  </body>
</html>
